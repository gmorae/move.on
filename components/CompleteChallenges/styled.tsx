import styled from 'styled-components';

export const ContainerCompleteChallenges = styled.header`
    display: flex;
    align-items: center;
    justify-content: space-between;

    margin: 3.5rem 0;
    padding-bottom: 1rem;
    border-bottom: 1px solid #d7d8da;

    font-weight: 500;
`;

export const Title = styled.span`
  font-size: 1.25rem;
`;

export const Numbers = styled.span`
  font-size: 1.5rem;
`;
